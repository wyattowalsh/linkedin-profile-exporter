# Apps Instructions

- Keep app-specific runtime behavior inside the owning app.
- Consume shared extraction, schema, settings, and exporter code from `@linkedin-profile-exporter/core`.
- Do not duplicate canonical schema or export-format definitions in app code.
