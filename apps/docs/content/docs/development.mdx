---
title: Development
description: Local development commands
---

# Development

Use `pnpm install`, `just quick`, and `just ci` for local validation. OpenSpec validation is available through `just openspec`.

Automated tests use local fixtures and local harness pages, not live LinkedIn accounts.
