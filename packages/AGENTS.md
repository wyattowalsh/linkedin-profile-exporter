# Packages Instructions

- Shared product contracts belong in packages, especially `packages/core`.
- Keep browser-extension APIs out of shared Node-loadable modules.
- Tests must use fixtures or local harness pages, not live LinkedIn accounts.
